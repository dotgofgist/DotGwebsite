# 출시단계 프롬프트 2 작업 결과

## 1. 시작 상태

* branch: `dev`
* 초기 Git 상태: 기존 untracked 문서 2개 있음
* Node: `v24.14.0`
* pnpm 요구 버전: `10.25.0` from `package.json`
* Supabase CLI: `2.109.1`
* 기존 project link: 있음

## 2. Production Supabase

* 연결 project: `DotG's Website`
* project ref: `qbwm...octq`
* region: `ap-northeast-2`
* status: `ACTIVE_HEALTHY`
* CLI authentication: PASS
* project link: PASS

secret/password/token/service role key는 출력하거나 저장하지 않았습니다.

## 3. Migration

| 항목 | 결과 |
| --- | --- |
| Local migration | 6개 확인 |
| Remote migration before | 4개 적용済, 2개 pending |
| Preflight | PASS |
| DB Push | PASS |
| Post-push 확인 | local/remote 6개 일치 |

적용한 migration:

* `20260724120000_harden_admin_data_integrity.sql`
* `20260724130000_grant_service_role_fixture_access.sql`

`db push` 후 Supabase CLI가 pg-delta cache 경고를 냈지만, migration 적용은 성공했고 이후 remote migration list/smoke/RLS가 모두 PASS였습니다.

## 4. Production Seed

* 적용 파일: `supabase/seeds/production.sql`
* 결과: PASS
* fake projects/notices/recruitments/contacts/social links: 0건 확인
* idempotency: `site_settings`에 `on conflict (id) do nothing`
* development seed `supabase/seed.sql`: Production에 적용하지 않음

## 5. Remote Schema

PASS 확인:

* tables: `profiles`, `projects`, `project_members`, `project_links`, `notices`, `recruitments`, `recruitment_steps`, `site_settings`, `contact_items`, `social_links`
* enum/type: PASS
* RPC/functions: `can_manage_content`, `is_admin`, `create_recruitment`, `save_recruitment`
* RLS: PASS
* Storage buckets: `project-images`, `site-assets`
* Storage policies: public read, editor/admin write policy 확인

## 6. Remote 검증

| 검증 | 결과 |
| --- | --- |
| Remote Read Smoke | PASS |
| Remote RLS | PASS |
| Storage Infrastructure | PASS |
| Storage paths/dangling/orphans | PASS |
| Type Generation | PASS |
| Type Diff | generated type update 발생 |

## 7. Production 환경 변수 이름

Public:

* `NEXT_PUBLIC_SUPABASE_URL`
* `NEXT_PUBLIC_SUPABASE_ANON_KEY`
* optional: `NEXT_PUBLIC_VERCEL_ENV`

Server/Smoke:

* `SMOKE_BASE_URL`
* `SMOKE_ALLOW_PRODUCTION`
* `VERCEL_ENV`
* `VERCEL_PROJECT_PRODUCTION_URL`

Supabase CLI / CI:

* `SUPABASE_ACCESS_TOKEN`
* `SUPABASE_PROJECT_REF_PRODUCTION`
* `SUPABASE_DB_PASSWORD_PRODUCTION`

Vercel/GitHub 이후 등록 필요:

* `VERCEL_TOKEN`
* `VERCEL_ORG_ID`
* `VERCEL_PROJECT_ID`

## 8. 변경 파일

이번 작업으로 변경된 파일:

* `dotg-web/src/lib/supabase/database.types.ts`

변경 내용은 remote schema 기준 Supabase generated type 갱신입니다. `__InternalSupabase.PostgrestVersion: "14.5"` 메타데이터가 추가됐습니다.

기존 untracked 문서 2개는 제가 만들거나 수정하지 않았고 그대로 두었습니다.

## 9. 보안 확인

* hardcoded secret: 없음
* tracked `.env`: 없음
* access token 노출: 없음
* service role key 노출: 없음
* `pnpm run secrets:scan`: PASS

## 10. 발견 문제

BLOCKER: 없음  
HIGH: 없음  
MEDIUM: 없음  
LOW: `git diff --check`에서 Windows line-ending warning만 표시됨. whitespace error는 없음.

## 11. 다음 단계 사용자 작업

프롬프트 3 전에 필요한 작업:

* Supabase Dashboard에서 Production Auth user 생성 또는 초대
* bootstrap-admin 절차로 첫 admin profile 지정
* Supabase Auth Site URL / Redirect URLs 확인
* 실제 admin 로그인 및 Storage 업로드 통합 검증

## 12. 판정

`PROMPT 2 COMPLETE`

DotG Production Supabase DB/schema/RLS/Storage infrastructure가 실제 remote project에 적용됐고, 다음 단계에서 Production 관리자 계정과 Auth/Storage 통합 검증을 진행할 수 있습니다.

```text
Git commit/push: 수행하지 않음
Vercel Production deploy: 수행하지 않음
Destructive remote operation: 수행하지 않음
```