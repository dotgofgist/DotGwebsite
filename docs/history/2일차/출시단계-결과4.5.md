`dotg-web/vercel.json`을 새로 추가했습니다.

```json
{
  "git": {
    "deploymentEnabled": false
  }
}
```

검증 결과:
* JSON parse: PASS
* `pnpm lint`: PASS
* `pnpm build`: PASS

Git 상태:
* `?? dotg-web/vercel.json`
* `?? docs/history/2일차/출시단계-프롬프트4.5.md` 는 기존/외부 생성 문서로 보이며 건드리지 않았습니다.

`vercel.json`은 untracked 새 파일이라 `git diff`에는 아직 표시되지 않습니다. 변경 내용은 위 JSON 전체가 전부입니다.