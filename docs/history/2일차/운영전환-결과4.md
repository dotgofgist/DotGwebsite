작업 완료했습니다. Storage 이미지 정합성 강화 범위는 `dotg-web` 안에서만 적용했고, 바깥의 `docs/history/...프롬프트4.md` 미추적 파일은 건드리지 않았습니다.

**적용 내용**
- [storage.ts](C:/Users/user/projects/DotGwebsite/dotg-web/src/lib/supabase/storage.ts): bucket별 허용 path 검증, project/site 전용 path 생성, public URL 생성 차단, 안전 삭제 래퍼/경고 로깅 추가
- [storage-validation.ts](C:/Users/user/projects/DotGwebsite/dotg-web/src/lib/supabase/storage-validation.ts): JPEG/PNG/WebP signature + dimension metadata 검증 추가
- 프로젝트 썸네일/사이트 로고/Hero 액션: 업로드 전 DB 상태 확인, current-path 조건 저장, DB 실패 시 새 object rollback, 기존 object 삭제 시 prefix 소유권 검사 적용
- 프로젝트 삭제 시 thumbnail object 삭제도 동일한 path 검증 사용
- Storage 정합성 SQL 추가:
  - `storage:paths:check`
  - `storage:dangling:check`
  - `storage:orphans:check`
  - `storage:orphans:cleanup` 기본 dry-run, `-- --apply` 명시 시 10분 이상 orphan 삭제
- [docs/storage-management.md](C:/Users/user/projects/DotGwebsite/dotg-web/docs/storage-management.md) 등 운영 문서 업데이트
- [tests/storage-integrity.test.mts](C:/Users/user/projects/DotGwebsite/dotg-web/tests/storage-integrity.test.mts) 추가

**검증**
- `pnpm run test:storage` 통과
- `pnpm run test:supabase` 통과
- `pnpm run test:auth` 통과
- `pnpm run test:env` 통과
- `pnpm exec tsc --noEmit` 통과
- `pnpm run lint` 통과
- `pnpm run storage:paths:check` 결과 0건
- `pnpm run storage:dangling:check` 결과 0건
- `pnpm run storage:orphans:check` 결과 0건
- `pnpm run storage:orphans:cleanup` dry-run 결과 0건
- `pnpm supabase test db` 통과, 6 files / 121 tests
- placeholder Supabase env로 `pnpm build` 통과

원격 Supabase/Vercel 배포와 실제 orphan 삭제 `--apply`는 수행하지 않았습니다.