구현 완료했습니다. 프롬프트 7 범위에 맞춰 PR CI, 로컬 Supabase DB 검증, 타입 drift 검사, Playwright E2E, Vercel Preview, 수동 Production 배포 workflow와 운영 문서를 추가했습니다.

**구성한 것**
- `.github/workflows/ci.yml`
  - `Static Checks`, `Database Tests`, `Production Build`, `Playwright E2E`
  - PR/main push 대상, 최소 권한 `contents: read`, concurrency 적용
  - E2E 실패 시 `playwright-report`, `test-results/e2e` artifact 업로드
- `.github/workflows/preview.yml`
  - same-repo PR만 Vercel Preview 배포
  - fork PR은 secret 보호 때문에 배포 skip
  - 배포 후 public read-only smoke 실행
- `.github/workflows/production.yml`
  - `workflow_dispatch` 전용
  - `production` GitHub Environment 승인 전제
  - main branch 제한, Supabase remote preflight, `supabase db push`, Vercel production deploy, read-only smoke
- `.github/workflows/dependency-review.yml`, `.github/dependabot.yml`
  - dependency review와 주간 dependency/action update PR 준비
- Node/pnpm 고정
  - `.nvmrc`, `.node-version`
  - `packageManager: pnpm@10.25.0`
  - Node engine `>=22 <25`
- CI 재현 스크립트
  - `ci:static`, `ci:database`, `ci:build`, `ci:e2e`, `ci:all`
  - `supabase:types:check`
  - `secrets:scan`
  - `smoke:preview`, `smoke:production`
- 문서
  - `docs/ci-cd.md`
  - `README.md`, `docs/e2e-testing.md`, `.env.ci.example` 갱신

**검증 결과**
- `pnpm install --frozen-lockfile`: PASS
- `pnpm run ci:static`: PASS
- `pnpm run ci:database`: PASS
  - Supabase DB tests: 7 files / 137 tests PASS
  - type drift check PASS
  - storage/RLS smoke PASS
- `pnpm run ci:build`: PASS
- `pnpm run ci:e2e`: PASS
  - Playwright: 35 passed / 0 failed
- secret scan: PASS

**주의할 점**
- GitHub Actions 자체와 Vercel Preview/Production 배포는 실제 PR push와 GitHub/Vercel secrets가 필요해서 로컬에서는 실행 확인하지 않았습니다.
- Production 상태는 `PARTIALLY READY`입니다. workflow와 runbook은 준비됐지만, GitHub `preview`/`production` Environment, required reviewers, branch protection, Vercel project secrets 설정이 아직 수동으로 필요합니다.
- `docs/history/...운영전환-프롬프트7.md`는 기존처럼 루트에 untracked로 남아 있습니다. 제가 만든 CI 산출물인 `.pnpm-store`, `blob-report`는 정리했습니다.