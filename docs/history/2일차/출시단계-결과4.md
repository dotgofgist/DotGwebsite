# 출시단계 프롬프트 4 작업 결과

## 1. 시작 상태
* branch: `dev`
* Git remote: `https://github.com/dotgofgist/DotGwebsite.git`
* GitHub repo: `dotgofgist/DotGwebsite`, default branch `main`
* Node: local `v24.14.0`, repo 기준 `.nvmrc`/`.node-version` = `22`
* pnpm: `10.25.0` from `package.json`
* Vercel CLI: `56.5.0`

## 2. GitHub Actions
| 항목 | 상태 |
| --- | --- |
| CI workflow | 존재, PR/main push 대상 |
| Preview workflow | 존재, same-repo PR만 Vercel deploy |
| Fork protection | PASS, fork PR은 deploy skip |
| Preview Environment | workflow에서 `environment: preview` 사용 |
| Required secrets | `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID` |

GitHub repository admin 권한/`gh` CLI가 없어 Environment secret 존재 여부, branch protection은 직접 확정하지 못했습니다.

## 3. Vercel Project
* `.vercel/project.json`: 없음
* `.vercel`: `.gitignore`에 포함됨
* Vercel CLI auth/link 상태: 확인 불가
* `vercel whoami`: user config/cache 접근 후 응답 없이 timeout

따라서 Vercel project 생성/연결은 사용자 Dashboard 작업이 필요합니다.

## 4. Preview 환경 변수
| Variable | Preview | Production | Source |
| --- | --- | --- | --- |
| `NEXT_PUBLIC_SUPABASE_URL` | 필요 | 필요 | Vercel env |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 필요 | 필요 | Vercel env |
| `NEXT_PUBLIC_SITE_URL` | 선택 | 권장 | Vercel env |
| `VERCEL_ENV` | 자동 | 자동 | Vercel |
| `VERCEL_PROJECT_PRODUCTION_URL` | 자동 가능 | 자동 가능 | Vercel |

Preview GitHub secrets는 Vercel deploy용 `VERCEL_*` 3개만 필요합니다. Supabase service role key는 Preview에 넣지 않습니다.

## 5. 변경 파일
`dotg-web/scripts/smoke-url.mts`

Preview smoke에서 mock fixture 상세 route 2개를 제거했습니다.

* removed `/projects/project-aurora`
* removed `/notices/website-operation-guide`

이유: Production Supabase는 현재 public content 0건이므로, Preview env가 정상일수록 해당 mock slug는 404가 되어 smoke가 잘못 실패할 수 있습니다.

## 6. 검증 결과
| 검증 | 결과 |
| --- | --- |
| TypeScript | PASS |
| Lint | PASS |
| Build | PASS |
| `test:env` | PASS |
| `test:auth` | PASS |
| `test:supabase` | PASS |
| Secret Scan | PASS |
| `git diff --check` | PASS, line-ending warning만 있음 |

## 7. 사용자 작업 필요
서비스: Vercel

1. Vercel에서 GitHub repo `dotgofgist/DotGwebsite` 연결
2. Root Directory: `dotg-web`
3. Framework: Next.js
4. Node.js: 22
5. Preview env 설정:
   * `NEXT_PUBLIC_SUPABASE_URL`
   * `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   * optional `NEXT_PUBLIC_SITE_URL`

서비스: GitHub

1. Environment `preview` 생성 확인
2. `preview` environment secrets 설정:
   * `VERCEL_TOKEN`
   * `VERCEL_ORG_ID`
   * `VERCEL_PROJECT_ID`
3. same-repo PR 생성 후 Preview workflow 실행

공유하지 말아야 할 값: Vercel token, Supabase anon key 값, DB password, service role key.

## 8. 판정
`PROMPT 4 WAITING FOR USER ACTION`

```text
Vercel Production deploy: 수행하지 않음
Production domain 변경: 수행하지 않음
Production DB destructive operation: 수행하지 않음
Secret source 저장: 없음
Git commit/push by Codex: 수행하지 않음
```