프롬프트 5 범위에서 관리자 UI/데이터 무결성 보완을 적용했습니다. 원격 Supabase/Vercel에는 적용하지 않았고, 기존 migration은 수정하지 않았습니다. 새 migration만 추가했습니다.

**핵심 변경**
- 프로젝트/공지/사이트 설정 수정 폼에 `updated_at` 기반 optimistic concurrency 적용
  - 다른 관리자가 먼저 저장하면 “refresh and try again” 오류로 차단
- 프로젝트/공지 validation 정리
  - slug 소문자 정규화
  - tag/member/link trim 및 중복 제거
  - URL protocol 검증 강화
- 공통 submit 버튼 pending 문구를 `Saving...`으로 정리하고 `aria-disabled` 적용
- `FormField` 오류 메시지를 `role="alert"` + `aria-live="polite"`로 보완
- 새 DB migration 추가:
  - [20260724120000_harden_admin_data_integrity.sql](C:/Users/user/projects/DotGwebsite/dotg-web/supabase/migrations/20260724120000_harden_admin_data_integrity.sql)
  - published 콘텐츠 `published_at` 필수
  - current 모집은 published만 허용
  - site_settings singleton 삭제 차단 trigger
  - URL protocol DB check
  - 프로젝트 멤버 `name + role` 중복 방지 index
- DB/단위 테스트 추가
  - [admin_data_integrity.test.sql](C:/Users/user/projects/DotGwebsite/dotg-web/supabase/tests/database/admin_data_integrity.test.sql)
  - [admin-validation.test.mts](C:/Users/user/projects/DotGwebsite/dotg-web/tests/admin-validation.test.mts)
- README 및 운영 문서 갱신

**검증 결과**
- `pnpm.cmd run lint` 통과
- `pnpm.cmd exec tsc --noEmit` 통과
- `pnpm.cmd build` 통과
- `pnpm.cmd supabase db reset` 통과
- `pnpm.cmd supabase test db` 통과: 7 files / 137 tests
- `pnpm.cmd run test:env` 통과
- `pnpm.cmd run test:auth` 통과
- `pnpm.cmd run test:storage` 통과
- `pnpm.cmd run test:admin` 통과
- `pnpm.cmd run test:supabase` 통과
- `pnpm.cmd run supabase:migrations:check` 통과

참고로 `../docs/history/...5.md` 미추적 파일 2개는 작업 범위 밖이라 건드리지 않았습니다.