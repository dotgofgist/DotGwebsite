프롬프트 7 작업 완료했습니다. 사이트 설정, 연락처, SNS 관리가 mock/disabled UI에서 Supabase 기반 관리자 조회 및 CRUD 흐름으로 전환됐습니다. 커밋은 만들지 않았습니다.

**작업 전 상태**
- `site_settings`: `id = 1` singleton check, `name/title/description/short_description`, `updated_by`, timestamps 확인
- `contact_items`: `label/value/href/description/is_active/sort_order`, sort nonnegative 확인
- `social_links`: `platform/label/url/description/is_active/sort_order`, active requires URL constraint 확인
- RLS: 공개 SELECT, `editor/admin` 관리 정책과 `can_manage_content()` 확인
- 기존 관리자 설정은 config 기반 disabled form
- 작업 전 `lint`, `build` 성공, 순차 `tsc` 성공
- 로컬 Supabase 사용 가능, 기존 DB tests PASS

**생성한 파일**
- `src/features/settings/types.ts`
- `src/features/settings/admin-mappers.ts`
- `src/features/settings/admin-queries.ts`
- `src/features/settings/actions.ts`
- `src/features/settings/validation.ts`
- `src/features/settings/components/settings-submit-button.tsx`
- `src/features/settings/components/contact-item-list.tsx`
- `src/features/settings/components/contact-item-form.tsx`
- `src/features/settings/components/contact-item-delete-form.tsx`
- `src/features/settings/components/social-link-list.tsx`
- `src/features/settings/components/social-link-form.tsx`
- `src/features/settings/components/social-link-delete-form.tsx`
- `src/app/admin/(dashboard)/settings/contacts/new/page.tsx`
- `src/app/admin/(dashboard)/settings/contacts/[id]/edit/page.tsx`
- `src/app/admin/(dashboard)/settings/social-links/new/page.tsx`
- `src/app/admin/(dashboard)/settings/social-links/[id]/edit/page.tsx`
- `supabase/tests/database/site_settings_management.test.sql`
- `docs/site-settings-management.md`

**수정/삭제**
- 관리자 페이지: `/admin/settings`, `/admin`
- 공개 페이지/컴포넌트: Header, Footer, Hero, Club summary, Contact card, SNS banner
- 설정 query/form: `public-queries.ts`, `site-settings-form.tsx`
- 대시보드: `dashboard-summary.tsx`
- 삭제: 기존 mock 기반 `contact-settings-form.tsx`, `social-links-form.tsx`

**구현 내용**
- 관리자 설정 조회는 `admin-queries.ts`에서 Supabase 조회, 모든 함수에서 `requireContentManager()` 재검사
- 사이트 설정은 `id=1` 고정 `upsert`; row가 없으면 관리자 폼은 config 초기값 표시 후 저장 시 생성
- 연락처/SNS는 생성·수정·삭제, 활성/비활성, `sort_order` 관리 구현
- 연락처 공개 조건: `is_active = true`
- SNS 공개 조건: `is_active = true` + `url is not null`
- Contact URL은 `http/https/mailto`, SNS URL은 `http/https`만 허용
- URL 없는 활성 SNS는 서버 validation과 DB constraint로 차단
- 변경 후 revalidate: `/`, `/about`, `/contact`, `/admin`, `/admin/settings`
- Supabase 미설정 시 공개 페이지는 기존 config fallback 유지
- Supabase 설정 후 DB 조회 실패는 fallback으로 숨기지 않음

**관리자 대시보드**
- 사이트 설정 등록 상태 표시
- 활성 연락처/전체 연락처 통계 표시
- 활성 URL SNS/전체 SNS 통계 표시
- 프로젝트·공지·모집의 실제 DB 조회 흐름 유지
- 설정 mock 안내 제거

**DB/RLS 테스트**
- `site_settings` singleton constraint
- editor/admin 설정·연락처·SNS 관리 가능
- anon/member mutation 불가
- anon 공개 SELECT 조건 검증
- 연락처/SNS CRUD
- sort_order constraint
- 활성 SNS URL constraint

**검증 결과**
- `pnpm.cmd lint` 성공
- `pnpm.cmd build` 성공
- `pnpm.cmd exec tsc --noEmit` 성공
- `pnpm.cmd supabase db reset` 성공
- `pnpm.cmd supabase test db` 성공: `Files=4, Tests=88, Result: PASS`
- `pnpm.cmd run supabase:types` 성공

**참고**
- 실제 브라우저에서 관리자 계정으로 클릭 검증은 하지 않았습니다. 로컬 계정 정보를 갖고 있지 않아 DB/RLS/빌드 검증으로 확인했습니다.
- Service Role Key, 실제 secret, 실제 계정 정보 하드코딩 없음.
- Storage, 로고 업로드, Hero 이미지 업로드, 사용자 역할 관리는 구현하지 않았습니다.
- `src/config/site.ts`, `src/config/contact.ts`, `src/config/social.ts`는 Supabase 미설정 fallback으로 유지했습니다.