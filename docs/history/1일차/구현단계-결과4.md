프롬프트 4 작업을 진행했고, 이번에는 Docker/Supabase 로컬 스택을 살려서 실제 `database.types.ts` 생성까지 완료했습니다. 그 덕분에 이전 프롬프트 2/3에서 막혀 있던 타입 전제도 일부 해소했습니다.

**완료한 것**
- Docker Desktop 실행 후 `pnpm supabase start` 성공
- `pnpm supabase db reset` 성공
- `pnpm run supabase:types` 성공, [database.types.ts](C:/Users/user/projects/DotGwebsite/dotg-web/src/lib/supabase/database.types.ts) 실제 schema 타입으로 갱신
- Auth DAL의 `profiles.role` 조회 정상화
- 관리자 프로젝트 조회를 mock에서 Supabase로 전환
  - [admin-queries.ts](C:/Users/user/projects/DotGwebsite/dotg-web/src/features/projects/admin-queries.ts)
  - `requireContentManager()` 재검사 포함
  - draft/published/archived 모두 조회
- 관리자 프로젝트 mapper 추가
  - [admin-mappers.ts](C:/Users/user/projects/DotGwebsite/dotg-web/src/features/projects/admin-mappers.ts)
- 프로젝트 생성/수정/삭제 Server Action 추가
  - [actions.ts](C:/Users/user/projects/DotGwebsite/dotg-web/src/features/projects/actions.ts)
  - 모든 mutation에서 `requireContentManager()` 호출
  - 사용자 세션 Supabase client 사용
  - `created_by`, `updated_by`를 서버 세션 사용자 id로 기록
- 프로젝트 입력 검증 추가
  - [validation.ts](C:/Users/user/projects/DotGwebsite/dotg-web/src/features/projects/validation.ts)
  - slug, 상태, 공개 상태, 태그, 날짜, 멤버, 링크 URL protocol 검증
- ProjectForm을 실제 Server Action 기반으로 전환
- ProjectTable을 `AdminProject[]`, 공개 상태, 수정일, 삭제 UI에 맞게 전환
- 프로젝트 삭제 확인 UI 추가
- 관리자 프로젝트 목록/생성/수정 페이지를 실제 query/form으로 전환
- 관리자 대시보드 프로젝트 데이터만 실제 Supabase query로 전환
- 문서 추가: [project-management.md](C:/Users/user/projects/DotGwebsite/dotg-web/docs/project-management.md)

**중요한 한계**
- 프로젝트와 관계 데이터 저장은 현재 앱 레벨의 다단계 mutation입니다. `projects` 저장 후 `project_members`/`project_links`를 교체합니다.
- 별도 DB RPC로 감싼 완전한 단일 트랜잭션 저장 함수는 아직 추가하지 않았습니다.
- 실제 editor/admin 계정이 준비되어 있지 않아 브라우저에서 생성/수정/삭제 수동 검증은 하지 못했습니다.
- 공지/모집/사이트 설정 CRUD는 범위 밖이라 유지했습니다.

**검증 결과**
- `pnpm.cmd lint`: 성공
- `pnpm.cmd build`: 성공
- `pnpm.cmd exec tsc --noEmit`: 성공
- `pnpm.cmd supabase db reset`: 성공
- `pnpm.cmd supabase test db`: 성공, 2 files / 41 tests PASS
- `pnpm run supabase:types`: 성공

**보안 점검**
- Service Role Key 사용 안 함
- Secret 사용 안 함
- 실제 계정/비밀번호/token 파일에 저장 안 함
- 클라이언트 DB mutation 없음
- FormData의 작성자 UUID 신뢰 안 함
- filter 없는 update/delete 없음
- RLS 우회 없음

로컬 Supabase 시작 출력에 개발용 키가 표시됐지만, 코드에는 기록하지 않았고 보고에도 값을 옮기지 않았습니다. 커밋도 만들지 않았습니다.