프롬프트 5 작업 완료했습니다. 관리자 공지사항이 mock 기반이 아니라 Supabase 기반으로 조회/생성/수정/삭제되도록 연결했고, `content_manager` 권한 확인을 거친 Server Action 흐름으로 구성했습니다.

주요 변경:
- `notices` 관리자 조회 구현: `admin-queries.ts`, `admin-mappers.ts`
- 공지 생성/수정/삭제 Server Actions 추가: `actions.ts`
- 제목/슬러그/요약/본문/상태/pinned 검증 추가: `validation.ts`
- 관리자 공지 폼/목록 UI를 실제 CRUD에 연결
- 관리자 대시보드 최근 공지도 Supabase 데이터 사용
- 공지 운영 정책 문서 추가: `dotg-web/docs/notice-management.md`
- Supabase 타입 재생성 완료

검증 결과:
- `pnpm.cmd exec tsc --noEmit` 성공
- `pnpm.cmd lint` 성공
- `pnpm.cmd build` 성공
- `pnpm.cmd supabase db reset` 성공
- `pnpm.cmd supabase test db` 성공, `Files=2, Tests=41, Result: PASS`
- 소스/문서 범위에서 service role key나 토큰류 하드코딩 흔적 없음

한 가지 참고점은 실제 브라우저에서 관리자 계정으로 공지 CRUD를 클릭 검증하진 않았습니다. 로컬 Supabase와 빌드/타입/린트/DB 테스트 기준으로는 통과했습니다.