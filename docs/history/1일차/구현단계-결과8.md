프롬프트 8 작업 완료했습니다. 커밋은 만들지 않았습니다.

구현한 내용:
- Supabase Storage migration 추가: `project-images`, `site-assets` public bucket, MIME/크기 제한, Storage RLS 정책
- `site_settings.logo_path`, `site_settings.hero_image_path` 추가
- 프로젝트 대표 이미지 업로드/교체/삭제 UI 및 server action 추가
- 프로젝트 삭제 시 기존 thumbnail object 삭제 시도
- 사이트 설정 관리자에 로고/Hero 이미지 업로드/교체/삭제 UI 추가
- 공개 Header 로고, 메인 Hero 이미지 렌더링 연결
- DB에는 object path만 저장하고, query mapper에서 public URL 생성
- JPEG/PNG/WebP 서버 검증 추가: MIME + 파일 시그니처 확인, SVG 차단
- `next.config.ts`에 `NEXT_PUBLIC_SUPABASE_URL` 기반 Storage remote pattern 추가
- Storage 정책 DB 테스트와 문서 추가: `docs/storage-management.md`
- README / schema 문서 현재 상태 갱신

검증 결과:
- `pnpm.cmd lint` 성공
- `pnpm.cmd build` 성공
- `pnpm.cmd exec tsc --noEmit` 성공
- `pnpm.cmd supabase db reset` 성공
- `pnpm.cmd supabase test db` 성공: `Files=5, Tests=104, Result: PASS`
- `pnpm.cmd run supabase:types` 성공
- secret/service role key 하드코딩 검색 결과: 코드에는 없음. Supabase config의 주석/env placeholder만 검색됨

참고:
- 실제 브라우저에서 관리자 계정으로 이미지 업로드 클릭 검증은 하지 않았습니다.
- 현재 `git status`에는 `dotg-web` 밖의 `../docs/history/...프롬프트8.md` 수정도 보입니다. 이번 구현은 건드리지 않았고, 커밋도 만들지 않았습니다.