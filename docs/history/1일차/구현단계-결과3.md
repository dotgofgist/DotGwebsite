프롬프트 3 작업을 가능한 범위까지 구현했습니다. 단, 중요한 전제 하나가 아직 깨져 있습니다: `src/lib/supabase/database.types.ts`가 실제 DB 생성 타입이 아니라 placeholder라서 `profiles.role`을 타입 안전하게 조회하는 단계는 구현하지 않았습니다. `any`나 가짜 테이블 타입으로 우회하지 않고, 해당 지점은 명확한 설정 오류로 처리했습니다.

**구현한 것**
- Server Action 기반 로그인/로그아웃 구조 추가
- 로그인 입력 서버 검증 추가
- 안전한 `next` 복귀 경로 검증 추가, 외부 URL 차단
- `/admin` 보호 Proxy 추가
  - Supabase env 없음: `/admin/login?reason=not-configured`
  - 공개 경로는 통과
- 관리자 Dashboard Layout에서 `requireContentManager()` 재검사 연결
- `/admin/unauthorized` 페이지 추가
- 관리자 Header에 실제 manager 정보 표시/로그아웃 폼 연결 구조 추가
- 로그인 폼을 `useActionState` 기반 Client Component로 전환
- 인증 문서 추가: [docs/authentication.md](C:/Users/user/projects/DotGwebsite/dotg-web/docs/authentication.md)
- README에 관리자 인증 안내 추가

**생성한 주요 파일**
- `src/features/auth/actions.ts`
- `src/features/auth/server.ts`
- `src/features/auth/validation.ts`
- `src/features/auth/redirects.ts`
- `src/features/auth/components/login-submit-button.tsx`
- `src/features/auth/components/logout-form.tsx`
- `src/app/admin/unauthorized/page.tsx`
- `docs/authentication.md`

**제한 사항**
- 실제 `profiles` 조회는 아직 미구현입니다. 이유: `database.types.ts`에 `profiles`, `user_role` 타입이 없습니다.
- 그래서 실제 editor/admin 로그인 성공 흐름은 아직 완료라고 볼 수 없습니다.
- 로그인 성공 시 현재는 역할 확인을 진행할 수 없어 local signOut 후 “Database 타입 생성 필요” 오류를 반환합니다.
- Supabase DB test도 로컬 Postgres/Docker 연결 실패로 검증하지 못했습니다.

**검증 결과**
- `pnpm.cmd lint`: 성공
- `pnpm.cmd build`: 성공
- `pnpm.cmd exec tsc --noEmit`: 성공
- `pnpm.cmd supabase test db`: 실패, local Postgres 연결 불가
- `/admin/login`: `200`
- `/admin`: `307` → `/admin/login?reason=not-configured`

**보안 점검**
- Service Role 미사용
- Secret 미사용
- 실제 이메일/비밀번호/token 없음
- password/token/cookie 로그 없음
- `user_metadata` 역할 미사용
- Open Redirect 방지 로직 추가
- Proxy만으로 최종 권한 판단하지 않고 Layout guard도 연결

커밋은 만들지 않았습니다. 다음에 Docker/Supabase local DB가 정상화되고 실제 `database.types.ts`가 생성되면, `getCurrentContentManager()`의 `profiles` 조회를 완성해서 editor/admin 실제 로그인 흐름까지 닫으면 됩니다.