작업 완료했습니다. Supabase 로컬 DB 스키마/RLS/seed/test/docs 기반을 `dotg-web`에 구성했고, 기존 Next.js 앱의 mock-data 흐름은 건드리지 않았습니다.

**핵심 결과**
- Next.js: `16.2.11`
- Supabase JS: `@supabase/supabase-js ^2.110.8`, `@supabase/ssr ^0.12.3`
- Supabase CLI: `2.109.1`, 이미 devDependency로 설치되어 있었음
- `supabase/config.toml`: 이미 존재
- Docker CLI는 있으나 daemon 연결 실패: `dockerDesktopLinuxEngine` pipe 없음
- `pnpm supabase start` 실패로 `db reset`, `test db`, local type generation은 미검증
- `src/lib/supabase/database.types.ts`는 placeholder 유지

**생성/수정**
- Migration:
  - [20260723073310_create_initial_content_schema.sql](C:/Users/user/projects/DotGwebsite/dotg-web/supabase/migrations/20260723073310_create_initial_content_schema.sql)
  - [20260723073320_add_initial_rls_policies.sql](C:/Users/user/projects/DotGwebsite/dotg-web/supabase/migrations/20260723073320_add_initial_rls_policies.sql)
- Tables: `profiles`, `projects`, `project_members`, `project_links`, `notices`, `recruitments`, `recruitment_steps`, `site_settings`, `contact_items`, `social_links`
- Enums: `content_status`, `project_status`, `project_link_type`, `recruitment_status`, `user_role`
- RLS: 모든 public 테이블 활성화. `anon`은 published/active 공개 조회만, `member`는 쓰기 불가, `editor/admin`은 content 관리, `admin`만 profile role 관리.
- Seed: [seed.sql](C:/Users/user/projects/DotGwebsite/dotg-web/supabase/seed.sql)
  - 프로젝트 3개, 공지 4개, 모집 1개, 연락처 2개, SNS 4개
  - mock에 링크가 비어 있어 project_links는 생성하지 않음
  - SNS URL은 추측하지 않고 `null`, inactive 처리
  - 원본 mock 문자열이 저장소에서 mojibake 상태라 seed는 slug/count/status/date 중심으로 대응시키고 문서에 차이를 기록
- Tests:
  - [initial_schema.test.sql](C:/Users/user/projects/DotGwebsite/dotg-web/supabase/tests/database/initial_schema.test.sql): 31 checks
  - [rls_enabled.test.sql](C:/Users/user/projects/DotGwebsite/dotg-web/supabase/tests/database/rls_enabled.test.sql): 10 checks
- Docs:
  - [database-schema.md](C:/Users/user/projects/DotGwebsite/dotg-web/docs/database-schema.md)
  - README에 Supabase local/dev migration 원칙 추가
- package scripts 추가: `supabase:start`, `supabase:stop`, `supabase:status`, `supabase:reset`, `supabase:test`, `supabase:types`

**검증**
- 작업 전: `pnpm.cmd lint` 성공, `pnpm.cmd build` 성공. 병렬 실행한 초기 `tsc`는 `.next/types` 생성 타이밍 때문에 실패했으나, 순차 재실행에서는 성공.
- 작업 후:
  - `pnpm.cmd lint`: 성공
  - `pnpm.cmd build`: 성공
  - `pnpm.cmd exec tsc --noEmit`: 성공
  - `pnpm.cmd supabase start`: 실패, Docker daemon/pipe 없음
- Secret/API key 검색: 실제 키 없음. Supabase 기본 config의 `env(...)` placeholder와 lockfile hash만 검색됨.
- 커밋하지 않았습니다.

**Git 상태**
- 기존부터 있던 변경: 루트 `docs/history/...`, `dotg-web/package.json`, `dotg-web/pnpm-lock.yaml`, `dotg-web/supabase/`
- 이번 작업 주요 변경: `dotg-web/supabase/`, `dotg-web/docs/database-schema.md`, `dotg-web/package.json`, `dotg-web/README.md`
- `.env.local` 생성/수정 없음

남은 TODO는 다음 페이즈 범위입니다: 공개 페이지 Supabase 조회 전환, Supabase Auth 로그인, 관리자 세션/Route 보호, CRUD, Storage, 원격 Supabase 연결, RLS 동작 통합 테스트 확대, Vercel 환경 변수, 운영 배포.