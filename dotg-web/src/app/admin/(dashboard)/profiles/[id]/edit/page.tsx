import { notFound } from "next/navigation"; import { ProfileForm } from "@/features/profiles/components/profile-form"; import { deleteProfileAction } from "@/features/profiles/actions"; import { getAdminProfile } from "@/features/profiles/queries"; import { Button } from "@/components/ui/button";
export default async function EditProfilePage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const profile = await getAdminProfile(id); if (!profile) notFound(); return <div><h1 className="mb-8 text-3xl font-bold">{profile.name} 프로필 편집</h1><ProfileForm mode="edit" profile={profile}/><form action={deleteProfileAction} className="mt-6"><input type="hidden" name="id" value={profile.id}/><input type="hidden" name="slug" value={profile.slug}/><Button type="submit" variant="secondary">프로필 삭제</Button></form></div>; }
