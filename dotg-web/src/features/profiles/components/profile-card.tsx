import Image from "next/image"; import Link from "next/link"; import { Badge } from "@/components/ui/badge"; import type { MemberProfile } from "../types";
export function ProfileCard({ profile }: { profile: MemberProfile }) { return <Link className="group aspect-square overflow-hidden rounded-xl border border-border bg-surface shadow-sm transition hover:-translate-y-1 hover:border-primary" href={`/profiles/${profile.slug}`}><article className="flex h-full flex-col"><div className="relative min-h-0 flex-1 bg-background">{profile.imageUrl ? <Image alt={`${profile.name} 프로필`} className="object-cover" fill sizes="(max-width: 768px) 100vw, 33vw" src={profile.imageUrl} unoptimized /> : <div className="flex h-full items-center justify-center text-6xl font-bold text-primary" aria-hidden="true">{profile.name.charAt(0)}</div>}</div><div className="space-y-2 p-5"><div><h2 className="text-xl font-semibold group-hover:text-primary">{profile.name}</h2><p className="text-sm text-neutral-500">{profile.position}</p></div><p className="line-clamp-2 text-sm text-neutral-600 dark:text-neutral-300">{profile.summary}</p><div className="flex gap-1 overflow-hidden">{profile.skills.slice(0, 3).map((skill) => <Badge key={skill}>{skill}</Badge>)}</div></div></article></Link>; }
